<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <template id="portal_my_home_customer_subscription" name="Portal My Home : Customer Subscription" inherit_id="portal.portal_my_home" priority="30">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="inside">
            <t t-call="portal.portal_docs_entry">
                <t t-set="title">My Subscriptions</t>
                <t t-set="url" t-value="'/my/subscriptions'" />
                <t t-set="count" t-value="subscription_count" />
            </t>
        </xpath>
    </template>

    <template id="portal_layout_customer_subscription" name="Portal Layout : Customer Subscription" inherit_id="portal.portal_breadcrumbs" priority="25">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'subscription' or subscription" t-attf-class="breadcrumb-item #{'active' if subscription else ''}">
                <a t-if="subscription" t-attf-href="/my/subscriptions?{{ keep_query() }}">My Subscriptions</a>
                <t t-else="">Subscriptions</t>
            </li>
            <li t-if="subscription" class="breadcrumb-item active">
                <t t-esc="subscription.name" />
            </li>
        </xpath>
    </template>

    <template id="portal_my_subscriptions" name="My Subscriptions">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True" />
            <t t-call="portal.portal_searchbar">
                <t t-set="title">Subscriptions</t>
            </t>
            <div class="form-inline ml-lg-4" t-if="request.env.user.employee_id">
                <a href="/my/subscriptions/new" class="btn btn-primary btn-sm" title="Open My Service" aria-label="Open My Service">
                    <i class="fa fa-plus"></i> Open My Service
                </a>
            </div>
            <t t-if="not subscriptions">
                <p>There are currently no subscriptions for your account.</p>
            </t>
            <t t-if="subscriptions" t-call="portal.portal_table">
                <thead>
                    <tr class="active portal_my_subscriptions_list">
                        <th>Name</th>
                        <th>Description</th>
                        <th class="text-right">Start Date</th>
                        <th class="text-right">Next Invoice Date</th>
                        <th class="text-right">Status</th>
                    </tr>
                </thead>
                <t t-foreach="subscriptions" t-as="subscription">
                    <tr>
                        <td>
                            <a t-attf-href="/my/subscriptions/#{subscription.id}?{{ keep_query() }}">
                                <t t-esc="subscription.name" />
                            </a>
                        </td>
                        <td>
                            <span t-esc="subscription.description" />
                        </td>
                        <td class="text-right">
                            <span t-field="subscription.start_date" />
                        </td>
                        <td class="text-right">
                            <span t-field="subscription.next_invoice_date" />
                        </td>
                        <td class="text-right">
                            <span t-field="subscription.status" />
                        </td>
                    </tr>
                </t>
            </t>
        </t>
    </template>
</odoo>
